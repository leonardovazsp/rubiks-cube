'''
API to interact with the Rubik's Cube and get images from the camera as well as the cube state.
The API provides the following endpoints:
- `/`: returns the cube state
- `/rotate`: rotates the cube
- `/reset`: resets the cube
- `/scramble`: scrambles the cube
- `/solve`: solves the cube
- `/image`: returns the image of the cube
- `/label`: returns the label of the cube
- `/random`: returns a random cube

The API is implemented using Flask.
'''

